{
  "name": "activity-box",
  "version": "1.0.0",
  "description": "Adds your recent activity to a github gist",
  "scripts": {
    "test": "echo 'no testing'",
    "format": "prettier --write ./index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/drewbi/activity-box.git"
  },
  "dependencies": {
    "@actions/core": "^1.9.0",
    "@actions/github": "^5.0.3"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "jest": "^26.0.1",
    "nock": "^12.0.3",
    "prettier": "^2.0.5",
    "pretty-quick": "^2.0.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  },
  "jest": {
    "setupFiles": [
      "<rootDir>/tests/setup.js"
    ]
  }
}
